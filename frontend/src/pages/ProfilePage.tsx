import React from "react";

const ProfilePage: React.FC = () => {
  return (
    <div>
      <h2>User Profile</h2>
      {/* Display user info and an option to update profile */}
    </div>
  );
};

export default ProfilePage;
